<style>

body {
    margin: 0;
    padding: 0;
	font-family: serif, arial, sans-serif;
}

/* container for picture, text, progress bars and buttons for each page of the story */
.page {
    margin: 0;
    padding: 0;
	padding-top: 10px;
	width: 100%;
    border: none;
	position: relative;
}    

/* Specific layouts for main picture and text blocks for horizontal and vertical page formats */
.side div.page div.pict {
    background-color: #ffffff;
    text-align: center;
    width: 45%;
    float: left;
    padding: 0px;	      
}
.side div.page div.text {
    background-color: #ffffff;
    width: 55%;
    float: right;
	text-align: center;
    font-size: 1.0em;
	padding-left: 0px;
	padding-right: 0px;
	padding-top: 20px;
	padding-bottom: 0px;
	font-family: inherit;
}

.under div.page div.pict {
    background-color: #ffffff;
    text-align: center;
    width: 100%;
    float: none;
    padding: 0px;	      
}
.under div.page div.text {
    background-color: #ffffff;
    width: 100%;
    float: none;
	text-align: center;
    font-size: 1.0em;
	padding-left: 0px;
	padding-right: 0px;
	padding-top: 0px;
	padding-bottom: 0px;
	font-family: inherit;
}

/* Specific text line table formats */
table.texttab {
	margin: auto;
/*	border: 1px solid black;  */
    border-collapse: collapse;
    padding-top: 0px;
	padding-bottom: 0px;
	padding-right: 0px;
	padding-left: 0px;
	font-family: inherit;
}
table.texttab td {	/*Space between paragraphs (paragraph = one or more text lines invoked by a single Play button) */
	padding-right: 10px;
	padding-bottom: 25px;
}

/* Button layouts */
div.underpic img { /*Positioning of navigation buttons in a vertical format page */
	vertical-align: middle;
}
td.button {	/*Positioning of Play buttons within table cell */
	vertical-align: top;
}
td.button img {	/*Size of Play buttons */
	height: 30px;
	width: 30px;
	padding-right: 0px;
	padding-left: 0px;
}

/* Spacing control for Fwd and Back buttons in all page formats */
img.btnback {
	padding-right: 60px;
	padding-bottom: 0px;
}
img.btnfwd {
	padding-left: 60px;
	padding-bottom: 0px;
}

img.mainpic {
	padding-bottom: 10px;
}

div.sidepic .title { /* Format for title text under picture in horizontal page mode */
	text-align: center;
    font-size: 1.5em;
	padding-left: 0px;
	padding-right: 0px;
	padding-top: 10px;
	padding-bottom: 0px;
	font-family: inherit;
}
.footer { /* Format for title and page # under text in vertical page mode */
	text-align: center;
    font-size: 1.0em;
	padding: 0px;
	font-family: inherit;
}

/* Reset the default appearance of all Progress Bars */
progress {
    -webkit-appearance: none;
    -moz-appearance: none;
    appearance: none;
}

progress[value] {	/* Determinate */
    -webkit-appearance: none;
    -moz-appearance: none;
    appearance: none;
}

progress:not([value]) {	/* Indeterminate */
    -webkit-appearance: none;
    -moz-appearance: none;
    appearance: none;
}

/* Set the required appearance of Progress Bars */
progress {	/* For Firefox (background) */
    background-color: #EEE;
	border: 1px solid #EEE; 
/*	border: none;  */
	vertical-align: top;
	height: 5px; 
}

progress::-moz-progress-bar {  /* For Firefox (value bar) */
	background-color: #CC4444;
	border-radius: 5px;
	height: 10px;
}

progress::-webkit-progress-bar {  /* For Webkit (background) */
	background: #EEE;
	height: 5px; 
	border: 1px solid #EEE;
	border-radius: 5px;
}
progress::-webkit-progress-value {  /* For Webkit (value bar) */
	background-color: #CC4444;
	border-radius: 5px;
	height: 10px;
}

</style>
